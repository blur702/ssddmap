{
  "name": "kml",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:ci": "playwright test --reporter=html",
    "test:headed": "playwright test --headed",
    "lint": "echo 'Linting JavaScript files...' && find public/js -name '*.js' -exec node -c {} \\;",
    "audit": "npm audit --audit-level moderate",
    "health": "curl -f http://localhost:3001 || exit 1",
    "pretest": "npm run health",
    "hooks:install": "./install-hooks.sh",
    "hooks:uninstall": "./uninstall-hooks.sh",
    "hooks:test": "echo 'Testing hooks...' && npm run lint && npm run health",
    "commit:safe": "git add -A && git commit",
    "commit:force": "git add -A && git commit --no-verify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@mapbox/togeojson": "^0.16.2",
    "@turf/turf": "^7.2.0",
    "dotenv": "^17.1.0",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0",
    "pg": "^8.16.3",
    "sqlite3": "^5.1.7",
    "wellknown": "^0.5.0",
    "xml2js": "^0.6.2",
    "xmldom": "^0.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "playwright": "^1.54.1"
  }
}
